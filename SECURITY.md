# Security notes

이 저장소는 공개 검토용 DEMO 모드로 배포할 수 있습니다. 실명·이메일·전화번호·사번·실응답을 넣지 마세요.

## 원본에서 제거한 위험

- 원본 Next.js/Supabase 코드와 Node lockfile을 포함하지 않았습니다.
- 원본 ZIP에 있던 `.env` 파일과 모든 로컬 비밀 설정을 제외했습니다.
- 비밀번호 없는 최고관리자 데모 로그인, 공개 AI API 프록시, 접두어 토큰 검색을 이식하지 않았습니다.
- 임의 규준 T점수·백분위와 실명 순위·히트맵을 제거했습니다.

원본 ZIP이 이미 공유되었으므로 그 안의 환경파일에 실제 값이 있었다면 관련 키·비밀번호를 회전해야 합니다.

## 실제 운영 전 필수

1. 외부 PostgreSQL과 서버측 인증을 연결합니다. Streamlit 세션 상태나 로컬 파일을 영속 저장소로 사용하지 않습니다.
2. 조직 ID 소유권을 모든 서버 쿼리에서 강제하고, 화면 숨김만으로 권한을 구현하지 않습니다.
3. 참여 링크는 128비트 이상의 난수 원문을 한 번만 제공하고 DB에는 해시만 저장합니다. 정확 일치·만료·폐기·시도 제한을 적용합니다.
4. 필수 개인정보 처리 동의와 선택적 개인결과 HR 공유 동의를 분리하고 철회 이력을 남깁니다.
5. 프로젝트 시작 시 문항·채점·매핑 버전을 스냅샷으로 고정합니다.
6. 응답 제출·채점·세션 완료를 하나의 트랜잭션으로 처리합니다.
7. N<5 억제와 역산 방지용 보완 억제를 적용합니다.
8. 비밀은 Streamlit Secrets 또는 별도 Secret Manager에만 둡니다.
