{
  "name": "kma-tap-cloudflare-open",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.32.0"
  }
}
